# API keys (never commit real values)
GOOGLE_API_KEY=
OPENAI_API_KEY=

# Optional model / timeout overrides
# Prefer a Gemini lite model if free-tier RPM is tight:
# ORCHESTRATOR_MODEL=gemini-3.1-flash-lite
# Workers currently expect an OpenAI-compatible WORKER_MODEL (needs billing credits):
# WORKER_MODEL=gpt-5.4-mini
# WORKER_TIMEOUT_S=300
# QA_TIMEOUT_S=150
# QA_HEADLESS=1
